# v1.1 功能增强 实现计划

> **面向 AI 代理的工作者：** 使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为 v1.0 新增三个增强功能：缴费方式加微信/支付宝、仪表盘增加各村收缴率排行榜、仪表盘增加异常抄表提醒列表

**架构：** 纯增量改动——后端新增 2 个 API + 1 个 DTO，前端修改 Dashboard.vue 和 Billing.vue。无数据库变更，无新实体/Repository。

**技术栈：** Spring Boot 4.0.6, Vue 3 + Element Plus, SQLite

---

### 任务 1：后端 — 异常抄表提醒 API

**文件：**
- 修改：`src/main/java/com/example/watermanagement/service/ReadingService.java` — 加接口方法
- 修改：`src/main/java/com/example/watermanagement/service/impl/ReadingServiceImpl.java` — 加实现
- 修改：`src/main/java/com/example/watermanagement/controller/ReadingController.java` — 加端点

- [ ] **步骤 1：ReadingService 接口新增方法**

在 `ReadingService.java` 文件末尾的 `}` 前添加：

```java
    /**
     * 查询最近异常抄表记录（用于仪表盘提醒）
     *
     * @param limit 返回条数上限
     * @return 异常抄表列表，含户名和村名
     */
    List<Map<String, Object>> getAbnormalReadings(int limit);
```

- [ ] **步骤 2：ReadingServiceImpl 实现查询**

在 `ReadingServiceImpl.java` 文件末尾的 `}` 前添加实现：

```java
    @Override
    public List<Map<String, Object>> getAbnormalReadings(int limit) {
        // JPQL join：异常 readings 关联 households 获取户名和村名
        String jpql = """
            SELECT new map(
                r.readingDate as readingDate,
                r.abnormalReason as abnormalReason,
                h.householdName as householdName,
                h.villageName as villageName
            )
            FROM Reading r
            JOIN Household h ON r.waterMeterId = h.waterMeterId
            WHERE r.isAbnormal = true
            ORDER BY r.readingDate DESC
            """;
        return entityManager.createQuery(jpql, (Class<Map<String, Object>>)(Class<?>)Map.class)
                .setMaxResults(limit)
                .getResultList();
    }
```

导入（加在文件顶部 import 区域）：
```java
import jakarta.persistence.EntityManager;
```

注入（加在 `@RequiredArgsConstructor` 之前或与其他字段一起）：
```java
    private final EntityManager entityManager;
```

- [ ] **步骤 3：ReadingController 新增端点**

在 `ReadingController.java` 文件末尾的 `}` 前添加：

```java
    @Operation(summary = "查询异常抄表记录", description = "返回最近N条异常抄表，含户名村名，用于仪表盘提醒")
    @GetMapping("/abnormal")
    public ApiResponse<List<Map<String, Object>>> getAbnormal(
            @Parameter(description = "返回条数") @RequestParam(defaultValue = "20") int limit) {
        return ApiResponse.ok(readingService.getAbnormalReadings(limit));
    }
```

导入：
```java
import java.util.Map;
```

- [ ] **步骤 4：编译验证**

```bash
./mvnw compile -q
```
预期：编译成功，无错误。

- [ ] **步骤 5：Commit**

```bash
git add src/main/java/com/example/watermanagement/service/ReadingService.java \
        src/main/java/com/example/watermanagement/service/impl/ReadingServiceImpl.java \
        src/main/java/com/example/watermanagement/controller/ReadingController.java
git commit -m "feat: 新增异常抄表查询API GET /api/readings/abnormal"
```

---

### 任务 2：后端 — 各村收缴率排行榜 API

**文件：**
- 创建：`src/main/java/com/example/watermanagement/dto/VillageCollectionRateRow.java`
- 修改：`src/main/java/com/example/watermanagement/service/ReportService.java` — 加接口方法
- 修改：`src/main/java/com/example/watermanagement/service/impl/ReportServiceImpl.java` — 加实现
- 修改：`src/main/java/com/example/watermanagement/controller/ReportController.java` — 加端点

- [ ] **步骤 1：创建 DTO**

创建 `src/main/java/com/example/watermanagement/dto/VillageCollectionRateRow.java`：

```java
package com.example.watermanagement.dto;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.math.BigDecimal;

/**
 * 各村收缴率排行 — 仪表盘用
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class VillageCollectionRateRow {

    /** 村名 */
    private String villageName;

    /** 应收总额 */
    private BigDecimal totalCharge;

    /** 实收总额 */
    private BigDecimal totalPaid;

    /** 收缴率 (0.0 ~ 100.0) */
    private double collectionRate;
}
```

- [ ] **步骤 2：ReportService 接口新增方法**

在 `ReportService.java` 文件末尾的 `}` 前添加：

```java
    /**
     * 各村收缴率排行（按收缴率升序，差的排前面）
     */
    List<com.example.watermanagement.dto.VillageCollectionRateRow>
        getVillageCollectionRates(int year, int month);
```

- [ ] **步骤 3：ReportServiceImpl 实现**

在 `ReportServiceImpl.java` 文件末尾的 `}` 前添加实现：

```java
    @Override
    public List<VillageCollectionRateRow> getVillageCollectionRates(int year, int month) {
        // 查当月所有水费账单，关联 households 获取村名
        List<WaterBill> bills = waterBillRepository.findByBillYearAndBillMonth(year, month);

        // 构建 waterMeterId → villageName 映射
        List<Household> households = householdRepository.findByIsActiveTrue();
        Map<String, String> meterToVillage = households.stream()
                .collect(Collectors.toMap(Household::getWaterMeterId, Household::getVillageName, (a, b) -> a));

        // 按村分组统计
        Map<String, BigDecimal[]> villageStats = new HashMap<>(); // [0]=charge, [1]=paid
        for (WaterBill bill : bills) {
            String village = meterToVillage.getOrDefault(bill.getWaterMeterId(), "未知");
            villageStats.computeIfAbsent(village, k -> new BigDecimal[]{BigDecimal.ZERO, BigDecimal.ZERO});
            BigDecimal[] stats = villageStats.get(village);
            stats[0] = stats[0].add(bill.getWaterCharge() != null ? bill.getWaterCharge() : BigDecimal.ZERO);
            stats[1] = stats[1].add(bill.getActualWaterPaid() != null ? bill.getActualWaterPaid() : BigDecimal.ZERO);
        }

        // 计算收缴率并排序
        return villageStats.entrySet().stream()
                .map(e -> {
                    BigDecimal[] s = e.getValue();
                    double rate = s[0].compareTo(BigDecimal.ZERO) > 0
                            ? s[1].multiply(new BigDecimal("100")).divide(s[0], 1, RoundingMode.HALF_UP).doubleValue()
                            : 100.0;
                    return VillageCollectionRateRow.builder()
                            .villageName(e.getKey())
                            .totalCharge(s[0])
                            .totalPaid(s[1])
                            .collectionRate(rate)
                            .build();
                })
                .sorted(Comparator.comparingDouble(VillageCollectionRateRow::getCollectionRate))
                .collect(Collectors.toList());
    }
```

导入（加到 ReportServiceImpl.java 顶部已有 import 区域）：
```java
import com.example.watermanagement.dto.VillageCollectionRateRow;
import java.math.RoundingMode;
import java.util.Comparator;
```

引用的 `HouseholdRepository` 需注入（如果尚未注入的话）：
```java
// 检查类中是否已有 householdRepository 字段，如果没有则加上
private final HouseholdRepository householdRepository;
```
> 注意：`ReportServiceImpl` 已经有 `householdRepository` 字段（来自之前的代码），无需重复添加。

- [ ] **步骤 4：ReportController 新增端点**

在 `ReportController.java` 文件末尾的 `}` 前添加：

```java
    @Operation(summary = "各村收缴率排行榜", description = "按收缴率升序排列，差的排前面")
    @GetMapping("/village-collection-rate")
    public ApiResponse<List<com.example.watermanagement.dto.VillageCollectionRateRow>> getVillageCollectionRates(
            @Parameter(description = "年份") @RequestParam int year,
            @Parameter(description = "月份") @RequestParam int month) {
        return ApiResponse.ok(reportService.getVillageCollectionRates(year, month));
    }
```

- [ ] **步骤 5：编译验证**

```bash
./mvnw compile -q
```
预期：编译成功。

- [ ] **步骤 6：Commit**

```bash
git add src/main/java/com/example/watermanagement/dto/VillageCollectionRateRow.java \
        src/main/java/com/example/watermanagement/service/ReportService.java \
        src/main/java/com/example/watermanagement/service/impl/ReportServiceImpl.java \
        src/main/java/com/example/watermanagement/controller/ReportController.java
git commit -m "feat: 新增各村收缴率排行API GET /api/reports/village-collection-rate"
```

---

### 任务 3：前端 — Billing.vue 添加微信/支付宝缴费方式

**文件：**
- 修改：`frontend/src/views/Billing.vue` — 缴费方式从硬编码改为下拉选择

- [ ] **步骤 1：添加缴费方式下拉选择器**

在 `Billing.vue` 中找到 `doPay()` 函数内的 `paymentMethod: '现金'` 这行（约在第 111 行），需要替换为变量引用。

首先，在 `<script setup>` 的 `ref` 区域（约在第 69 行 `const payAmount = ref(0)` 后面）添加：

```javascript
const payMethod = ref('现金')
```

然后找到缴费汇总 div 中 `<el-button>` 前（约在第 50 行），在找零提示后面、支付按钮前面插入：

```html
        <el-select v-model="payMethod" style="width:100px">
          <el-option label="现金" value="现金" />
          <el-option label="微信" value="微信" />
          <el-option label="支付宝" value="支付宝" />
        </el-select>
```

最后在 `doPay()` 函数中（约在第 111 行），把：
```javascript
      paymentMethod: '现金',
```
改为：
```javascript
      paymentMethod: payMethod.value,
```

- [ ] **步骤 2：构建前端验证**

```bash
cd frontend && npm run build
```
预期：构建成功，无错误。

- [ ] **步骤 3：Commit**

```bash
git add frontend/src/views/Billing.vue
git commit -m "feat: 缴费方式支持微信和支付宝"
```

---

### 任务 4：前端 — Dashboard.vue 新增排行榜和异常提醒

**文件：**
- 修改：`frontend/src/views/Dashboard.vue` — 替换系统概览卡片
- 修改：`frontend/src/api/index.js` — 加两个 API 函数

- [ ] **步骤 1：api/index.js 新增 API 函数**

在 `frontend/src/api/index.js` 中 `reportApi` 对象里（约第 68 行）添加：

```javascript
  getVillageCollectionRates: (params) => api.get('/reports/village-collection-rate', { params }),
```

在 `readingApi` 对象里（约第 41 行）添加：

```javascript
  getAbnormal: (params) => api.get('/readings/abnormal', { params }),
```

- [ ] **步骤 2：Dashboard.vue 替换"系统概览"卡片为排行榜+异常提醒**

将 `Dashboard.vue` 中 `<template>` 里第 40-47 行的"系统概览"卡片：

```html
    <!-- 最近缴费记录 -->
    <el-card style="margin-top:20px" header="系统概览">
      <el-descriptions :column="2" border>
        <el-descriptions-item label="数据库位置">data/water_meter.db</el-descriptions-item>
        <el-descriptions-item label="水价">¥1.80 / 吨</el-descriptions-item>
        <el-descriptions-item label="后端端口">8080</el-descriptions-item>
        <el-descriptions-item label="前端端口">3000（开发模式）</el-descriptions-item>
      </el-descriptions>
    </el-card>
```

替换为：

```html
    <el-row :gutter="20" style="margin-top:20px">
      <!-- 各村收缴率排行 -->
      <el-col :span="14">
        <el-card header="各村收缴率排行" shadow="hover">
          <el-table :data="villageRates" stripe size="small" max-height="350">
            <el-table-column prop="villageName" label="村名" width="100" />
            <el-table-column prop="totalCharge" label="应收" width="100" />
            <el-table-column prop="totalPaid" label="实收" width="100" />
            <el-table-column label="收缴率" min-width="160">
              <template #default="{ row }">
                <div style="display:flex;align-items:center;gap:8px">
                  <el-progress
                    :percentage="row.collectionRate"
                    :color="row.collectionRate >= 80 ? '#67C23A' : row.collectionRate >= 50 ? '#E6A23C' : '#F56C6C'"
                    :stroke-width="16"
                    style="flex:1"
                  />
                  <span style="font-size:12px;width:45px">{{ row.collectionRate }}%</span>
                </div>
              </template>
            </el-table-column>
          </el-table>
        </el-card>
      </el-col>

      <!-- 异常抄表提醒 -->
      <el-col :span="10">
        <el-card header="异常抄表提醒" shadow="hover">
          <el-table :data="abnormalReadings" stripe size="small" max-height="350">
            <el-table-column prop="readingDate" label="日期" width="100">
              <template #default="{ row }">{{ row.readingDate?.slice(0, 10) }}</template>
            </el-table-column>
            <el-table-column prop="householdName" label="户名" width="80" />
            <el-table-column prop="villageName" label="村名" width="80" />
            <el-table-column prop="abnormalReason" label="异常原因" min-width="120" show-overflow-tooltip />
          </el-table>
          <el-empty v-if="!abnormalReadings.length" description="暂无异常" :image-size="60" />
        </el-card>
      </el-col>
    </el-row>
```

- [ ] **步骤 3：Dashboard.vue 更新 script 部分**

替换 `<script setup>` 中的 onMounted 以后部分（在第 62-79 行之后追加新数据加载）：

在 `onMounted` 函数末尾（第 79 行 `})` 后）追加：

```javascript
  // 加载各村收缴率
  try {
    const rates = await reportApi.getVillageCollectionRates({ year: now.getFullYear(), month: now.getMonth() + 1 })
    villageRates.value = rates || []
  } catch { /* 忽略 */ }

  // 加载异常抄表
  try {
    const abnormal = await readingApi.getAbnormal({ limit: 20 })
    abnormalReadings.value = abnormal || []
  } catch { /* 忽略 */ }
```

在 `const stats = reactive({...})` (约第 55-60 行) 后面添加：

```javascript
const villageRates = ref([])
const abnormalReadings = ref([])
```

在 import 语句（第 52 行）中把 `import { householdApi, reportApi } from '@/api'` 改为：

```javascript
import { householdApi, reportApi, readingApi } from '@/api'
```

在 `vue` 的 import 行把 `reactive, onMounted` 改为：

```javascript
import { reactive, ref, onMounted } from 'vue'
```

- [ ] **步骤 4：构建前端验证**

```bash
cd frontend && npm run build
```
预期：构建成功。

- [ ] **步骤 5：Commit**

```bash
git add frontend/src/views/Dashboard.vue frontend/src/api/index.js
git commit -m "feat: 仪表盘新增各村收缴率排行榜和异常抄表提醒"
```

---

### 任务 5：集成测试

- [ ] **步骤 1：启动后端**

```bash
./mvnw spring-boot:run
```
预期：`Started WaterManagementApplication`

- [ ] **步骤 2：测试 API**

```bash
# 测试异常抄表
curl http://localhost:8080/api/readings/abnormal?limit=5

# 测试各村收缴率
curl http://localhost:8080/api/reports/village-collection-rate?year=2026\&month=6
```
预期：两个都返回 `{"code":200,...}`

- [ ] **步骤 3：Commit 前端构建产物**

```bash
git add -A
git commit -m "chore: 重新构建前端（v1.1 仪表盘增强）"
```

---