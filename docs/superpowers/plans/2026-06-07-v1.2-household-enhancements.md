# v1.2 村民管理增强 实现计划

> **面向 AI 代理的工作者：** 使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为村民管理模块增加 Excel 批量导入/导出功能，并在村民详情页整合缴费历史记录

**架构：** 新增 1 个 Excel 行 DTO（HouseholdExportRow），后端 HouseholdController 加 2 个端点（export/import），前端加导入导出按钮 + 缴费历史表格。无数据库变更。

**技术栈：** Spring Boot 4.0.6, EasyExcel 4.0.3, Vue 3 + Element Plus

---

### 任务 1：后端 — Excel 导出 API

**文件：**
- 创建：`src/main/java/com/example/watermanagement/dto/HouseholdExportRow.java`
- 修改：`src/main/java/com/example/watermanagement/service/HouseholdService.java`
- 修改：`src/main/java/com/example/watermanagement/service/impl/HouseholdServiceImpl.java`
- 修改：`src/main/java/com/example/watermanagement/controller/HouseholdController.java`

- [ ] **步骤 1：创建 HouseholdExportRow DTO**

创建 `src/main/java/com/example/watermanagement/dto/HouseholdExportRow.java`：

```java
package com.example.watermanagement.dto;

import com.alibaba.excel.annotation.ExcelProperty;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.math.BigDecimal;

/**
 * 村民信息 Excel 导出/导入 行
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class HouseholdExportRow {

    @ExcelProperty("户主姓名")
    private String householdName;

    @ExcelProperty("联系电话")
    private String phone;

    @ExcelProperty("所属村名")
    private String villageName;

    @ExcelProperty("水表编号")
    private String waterMeterId;

    @ExcelProperty("材料费总额")
    private BigDecimal materialFeeTotal;
}
```

- [ ] **步骤 2：HouseholdService 接口新增方法**

在 `HouseholdService.java` 的 `}` 前添加：

```java
    /**
     * 导出村民数据到 Excel
     */
    void exportToExcel(List<String> villageNames, String waterMeterId,
                       jakarta.servlet.http.HttpServletResponse response) throws java.io.IOException;

    /**
     * 从 Excel 批量导入村民
     * @return 导入结果 {total: N, message: "..."}
     */
    java.util.Map<String, Object> importFromExcel(java.io.InputStream inputStream);
```

- [ ] **步骤 3：HouseholdServiceImpl 实现导出**

在 `HouseholdServiceImpl.java` 末尾的 `}` 前添加（注意：需要把 `OutputStream out` 那段从 `ExcelUtil` 的模式搬过来，但直接调用 `ExcelUtil.export()` 更简洁）：

```java
    @Override
    public void exportToExcel(List<String> villageNames, String waterMeterId,
                               jakarta.servlet.http.HttpServletResponse response) throws java.io.IOException {
        // 获取筛选后的数据（不分页，取全部）
        List<Household> households;
        if (villageNames != null && !villageNames.isEmpty()) {
            households = householdRepository.findByVillageNameInAndIsActiveTrue(villageNames);
        } else {
            households = householdRepository.findByIsActiveTrue();
        }

        // 转成导出 DTO
        List<com.example.watermanagement.dto.HouseholdExportRow> rows = households.stream()
                .map(h -> com.example.watermanagement.dto.HouseholdExportRow.builder()
                        .householdName(h.getHouseholdName())
                        .phone(h.getPhone())
                        .villageName(h.getVillageName())
                        .waterMeterId(h.getWaterMeterId())
                        .materialFeeTotal(h.getMaterialFeeTotal())
                        .build())
                .collect(java.util.stream.Collectors.toList());

        ExcelUtil.export(response, "村民信息", com.example.watermanagement.dto.HouseholdExportRow.class, rows);
        log.info("导出村民信息: {} 户", rows.size());
    }
```

- [ ] **步骤 4：HouseholdServiceImpl 实现导入**

继续在 `HouseholdServiceImpl.java` 的 `}` 前添加：

```java
    @Override
    @Transactional
    public java.util.Map<String, Object> importFromExcel(java.io.InputStream inputStream) {
        List<com.example.watermanagement.dto.HouseholdExportRow> rows =
                ExcelUtil.read(inputStream, com.example.watermanagement.dto.HouseholdExportRow.class);

        // ===== 全量唯一性校验 =====
        // 1. 检查 Excel 内部是否有重复水表编号
        java.util.Set<String> excelIds = new java.util.HashSet<>();
        java.util.List<String> conflicts = new java.util.ArrayList<>();
        for (int i = 0; i < rows.size(); i++) {
            String id = rows.get(i).getWaterMeterId();
            if (id == null || id.isBlank()) {
                conflicts.add("第" + (i + 1) + "行：水表编号为空");
            } else if (!excelIds.add(id)) {
                conflicts.add("第" + (i + 1) + "行：水表编号 " + id + " 在文件中重复");
            }
        }

        // 2. 检查数据库中是否已存在
        for (com.example.watermanagement.dto.HouseholdExportRow row : rows) {
            if (row.getWaterMeterId() != null && !row.getWaterMeterId().isBlank()
                    && householdRepository.existsByWaterMeterId(row.getWaterMeterId())) {
                conflicts.add("水表编号 " + row.getWaterMeterId() + "（" + row.getHouseholdName() + "）已在系统中存在");
            }
        }

        if (!conflicts.isEmpty()) {
            java.util.Map<String, Object> errorResult = new java.util.HashMap<>();
            errorResult.put("conflicts", conflicts);
            errorResult.put("message", "存在 " + conflicts.size() + " 个冲突，已中止导入");
            return errorResult;
        }

        // ===== 全部通过，批量写入 =====
        int count = 0;
        for (com.example.watermanagement.dto.HouseholdExportRow row : rows) {
            Household household = Household.builder()
                    .householdName(row.getHouseholdName())
                    .phone(row.getPhone())
                    .villageName(row.getVillageName())
                    .waterMeterId(row.getWaterMeterId())
                    .materialFeeTotal(row.getMaterialFeeTotal() != null
                            ? row.getMaterialFeeTotal()
                            : new java.math.BigDecimal("1500.00"))
                    .isActive(true)
                    .build();
            householdRepository.save(household);

            // 同时创建材料费账单
            materialBillRepository.save(MaterialBill.builder()
                    .waterMeterId(household.getWaterMeterId())
                    .totalFee(household.getMaterialFeeTotal())
                    .actualPaid(java.math.BigDecimal.ZERO)
                    .status("未收")
                    .build());
            count++;
        }

        log.info("批量导入村民: {} 户", count);
        java.util.Map<String, Object> result = new java.util.HashMap<>();
        result.put("total", count);
        result.put("message", "成功导入 " + count + " 户");
        return result;
    }
```

- [ ] **步骤 5：HouseholdController 新增两个端点**

在 `HouseholdController.java` 的 `}` 前添加：

```java
    @Operation(summary = "导出村民信息到Excel", description = "按当前筛选条件导出，无水表编号模糊匹配")
    @GetMapping("/export")
    public void export(
            @Parameter(description = "村名列表（多选）") @RequestParam(required = false) List<String> villageNames,
            @Parameter(description = "水表编号（模糊搜索）") @RequestParam(required = false) String waterMeterId,
            jakarta.servlet.http.HttpServletResponse response) throws java.io.IOException {
        householdService.exportToExcel(villageNames, waterMeterId, response);
    }

    @Operation(summary = "批量导入村民", description = "上传Excel文件，全量唯一性校验后批量写入")
    @PostMapping("/import")
    public ApiResponse<java.util.Map<String, Object>> importExcel(
            @Parameter(description = "Excel 文件") @RequestParam("file") org.springframework.web.multipart.MultipartFile file) throws java.io.IOException {
        java.util.Map<String, Object> result = householdService.importFromExcel(file.getInputStream());
        if (result.containsKey("conflicts")) {
            return ApiResponse.fail(result.get("message").toString());
        }
        return ApiResponse.ok(result.get("message").toString(), result);
    }
```

在文件顶部 import 区域补充：
```java
import org.springframework.web.multipart.MultipartFile;
```

- [ ] **步骤 6：编译验证**

```bash
./mvnw compile -q
```
预期：编译成功，无错误。

- [ ] **步骤 7：Commit**

```bash
git add src/main/java/com/example/watermanagement/dto/HouseholdExportRow.java \
        src/main/java/com/example/watermanagement/service/HouseholdService.java \
        src/main/java/com/example/watermanagement/service/impl/HouseholdServiceImpl.java \
        src/main/java/com/example/watermanagement/controller/HouseholdController.java
git commit -m "feat: 村民Excel批量导入导出 API"
```

---

### 任务 2：前端 — api/index.js 新增 API 函数

**文件：**
- 修改：`frontend/src/api/index.js`

- [ ] **步骤 1：添加 API 函数**

在 `householdApi` 对象中（约第 32 行）添加：

```javascript
  exportExcel: (params) => api.get('/households/export', { params, responseType: 'blob' }),
  importExcel: (formData) => api.post('/households/import', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  }),
```

- [ ] **步骤 2：Commit**

```bash
git add frontend/src/api/index.js
git commit -m "feat: 前端村民Excel导入导出API函数"
```

---

### 任务 3：前端 — Households.vue 添加导入导出按钮 + 缴费历史

**文件：**
- 修改：`frontend/src/views/Households.vue`

- [ ] **步骤 1：在村民表单上方加导入导出按钮**

在 `<h3>{{ form.id ? '编辑村民' : '新增村民' }}</h3>` 行和 `<el-button type="primary" @click="resetForm">新增</el-button>` 的 div 中，在"新增"按钮后追加：

```html
        <el-button type="success" @click="exportExcel">📥 导出Excel</el-button>
        <el-upload :before-upload="importExcel" :show-file-list="false" accept=".xlsx" style="display:inline-block;margin-left:8px">
          <el-button type="warning">📤 导入Excel</el-button>
        </el-upload>
```

- [ ] **步骤 2：在表单下方添加缴费历史表格**

在 `</el-form>` 之后、`</div>`（右侧内容区结束标签）之前添加：

```html
      <div v-if="form.id" style="margin-top:20px">
        <h4>缴费历史</h4>
        <el-table :data="paymentHistory" stripe size="small" max-height="300">
          <el-table-column prop="paidDate" label="日期" width="100" />
          <el-table-column prop="billType" label="类型" width="80">
            <template #default="{ row }">
              <el-tag :type="row.billType === 'water' ? 'primary' : 'warning'" size="small">
                {{ row.billType === 'water' ? '水费' : '材料费' }}
              </el-tag>
            </template>
          </el-table-column>
          <el-table-column prop="amount" label="金额" width="100" />
          <el-table-column prop="paymentMethod" label="方式" width="80" />
          <el-table-column prop="operator" label="操作员" width="80" />
          <el-table-column prop="note" label="备注" min-width="120" show-overflow-tooltip />
        </el-table>
        <el-empty v-if="!paymentHistory.length" description="暂无缴费记录" :image-size="40" />
      </div>
```

- [ ] **步骤 3：script 中添加数据和方法**

在 `<script setup>` 的 import 行，把：
```javascript
import { householdApi } from '@/api'
```
改为：
```javascript
import { householdApi, paymentApi } from '@/api'
```

在 `const deleting = ref(false)` 后面添加：

```javascript
const paymentHistory = ref([])
```

在 `onNodeClick` 函数末尾（`Object.assign(form, ...)` 之后、函数结束之前）添加：

```javascript
    loadPaymentHistory()
```

在 `resetForm` 函数中 `Object.assign(form, { id: null, ... })` 之后添加：

```javascript
    paymentHistory.value = []
```

在 `onMounted(loadTree)` 之前添加：

```javascript
async function loadPaymentHistory() {
  if (!form.id) { paymentHistory.value = []; return }
  try {
    paymentHistory.value = await paymentApi.getHistory(form.waterMeterId) || []
  } catch { paymentHistory.value = [] }
}

async function exportExcel() {
  const params = {}
  if (selectedVillages.value.length) params.villageNames = selectedVillages.value.join(',')
  const blob = await householdApi.exportExcel(params)
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url; a.download = '村民信息.xlsx'; a.click()
  URL.revokeObjectURL(url)
  ElMessage.success('导出成功')
}

async function importExcel(file) {
  const formData = new FormData()
  formData.append('file', file)
  try {
    const result = await householdApi.importExcel(formData)
    ElMessage.success(result?.message || '导入成功')
    loadTree()
  } catch (e) {
    ElMessage.error(e.message || '导入失败，请检查Excel格式')
  }
  return false
}
```

- [ ] **步骤 4：构建前端验证**

```bash
cd frontend && npm run build
```
预期：构建成功。

- [ ] **步骤 5：Commit**

```bash
git add frontend/src/views/Households.vue
git commit -m "feat: 村民管理导入导出按钮+缴费历史表格"
```

---

### 任务 4：集成测试

- [ ] **步骤 1：启动后端测试 API**

```bash
./mvnw spring-boot:run
```

- [ ] **步骤 2：测试导出**

```bash
curl -o test.xlsx http://localhost:8080/api/households/export
```
预期：下载 Excel 文件，包含现有村民数据。

- [ ] **步骤 3：测试导入**

```bash
curl -X POST http://localhost:8080/api/households/import -F "file=@test.xlsx"
```
预期：返回冲突信息（因为数据已存在）。

- [ ] **步骤 4：Commit 构建产物**

```bash
git add -A
git commit -m "chore: 前端v1.2构建产物"
```

---
